# TechSpeak

A self-contained learning dashboard for practicing technical English as a foreign language.

## Run

Open `index.html` in a browser, or serve the folder with:

```bash
python3 -m http.server 8000
```

Then visit http://localhost:8000.
